{
  "name": "absen-qr-hillaryours",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Landing page + QR scanner absensi fanbase hillaryours.id (JKT48). Frontend scan terpisah dari Apps Script (yang jadi JSON API).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@zxing/browser": "^0.1.5",
    "@zxing/library": "^0.21.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
